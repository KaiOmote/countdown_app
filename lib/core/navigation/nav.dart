import 'package:flutter/widgets.dart';

/// Global navigator key for navigation outside of widget context (e.g., notifications).
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
